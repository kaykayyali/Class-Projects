<h1> Welcome <%= @user.name %> </h1>


<ul>
   <% @products.each do |product| %>
   <li>
   		<a href="<%= user_product_path(@user.id, product.id)%>"> <%= product.title %></a>
   		- 
   		<%= product.description %>
   	</li>
   	<% end %>
   	</ul>